### [Problem Name]
**Link:** [URL]

- **Trigger:** (What keyword or constraint in the prompt told you to use this approach? e.g., "Sorted array + O(log n) requirement = Binary Search")
- **The Trick:** (The specific mechanical twist for this exact problem. e.g., "Use two pointers, but only move the left pointer if sum > target.")
- **Complexity:** Time `O()` | Space `O()`

**Approach:**
1. (Step 1)
2. (Step 2)

**Code:**
```python
# Insert concise code here
```

**Gotchas / Edge Cases:**
- (e.g., Array with all duplicate elements)
- (e.g., Out of bounds if pointer goes negative)
